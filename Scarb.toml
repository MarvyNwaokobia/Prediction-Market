[package]
name = "btc_prediction_market"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = ">=2.8.0"

[[target.starknet-contract]]
sierra = true
casm = true

[dev-dependencies]
snforge_std = { git = "https://github.com/foundry-rs/starknet-foundry", tag = "v0.56.0" }

[scripts]
test = "snforge test"
